country={{ country_code }} # Your 2-digit country code
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
network={
    ssid="{{ wifi_ssid }}"
    psk="{{ wifi_pass }}"
    key_mgmt={{ key_management | default ('WPA-PSK') }}
}
